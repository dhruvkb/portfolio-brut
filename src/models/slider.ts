export enum SliderContent {
  /**
   * the slider shows the CLI; this happens for larger screen sizes when the
   * split view can be accommodated
   */
  CLI='cli',
  /**
   * the slider shows details; this happens for smaller screen sizes when the
   * split view cannot be accommodated
   */
  DETAIL='detail'
}
